# Dependencias del proyecto
# Visualización de tablas en consola
rich>=13.0.0
# Peticiones HTTP a la API de ONPE
requests>=2.31.0
# Servidor web para la visualización
flask>=3.0.0

# --- Producción / seguridad ---
# Servidor WSGI para producción (no usar el servidor de desarrollo de Flask)
gunicorn>=21.2.0
# Límite de peticiones por IP (mitiga abuso / DoS)
flask-limiter>=3.5.0
# Cabeceras de seguridad y Content Security Policy
flask-talisman>=1.1.0
